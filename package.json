{
  "name": "liquid-ajax-cart",
  "version": "0.1.1",
  "description": "",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "webpack-watch": "webpack --mode=development --watch",
    "themekit-watch": "shopify-themekit open && shopify-themekit watch",
    "watch": "npm-run-all --parallel themekit-watch webpack-watch",
    "build": "rimraf docs/releases/last && webpack --mode=production && webpack --mode=production --env last=true"
  },
  "author": "Evgeniy Mukhamedjanov",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2",
    "webpack": "^5.53.0",
    "webpack-cli": "^4.8.0"
  },
  "dependencies": {}
}
