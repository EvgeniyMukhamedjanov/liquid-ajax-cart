{
  "name": "liquid-ajax-cart",
  "version": "0.0.1",
  "description": "",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "webpack-watch": "webpack --mode=development --watch",
    "themekit-watch": "shopify-themekit watch",
    "watch": "npm-run-all --parallel themekit-watch webpack-watch",
    "build": "webpack --mode=production && webpack --mode=production --env last=true"
  },
  "author": "Evgeniy Mukhamedjanov",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "npm-run-all": "^4.1.5",
    "webpack": "^5.53.0",
    "webpack-cli": "^4.8.0"
  },
  "dependencies": {
    "htm": "^3.1.0",
    "preact": "^10.5.14"
  }
}
